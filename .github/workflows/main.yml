name: ci
on: [push, pull_request]
jobs:
  check:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: actions/setup-python@v4
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install .[testing,qa] coverage
      - name: flake8
        run: python -m flake8 jedi setup.py
      - name: mypy
        run: python -m mypy jedi sith.py setup.py
      - name: jedi + coverage
        run: python -m coverage run --source jedi -m pytest
      - name: coverage report
        run: python -m coverage report
