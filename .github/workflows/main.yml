name: test
on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        uses: nicledomaS/cmake_build_action@v1.4
        with:
          config: Release
      - name: Run
        run: ./build/unittests
        shell: bash
