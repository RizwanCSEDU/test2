on:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: "ABC"
      AWS_SECRET_ACCESS_KEY: "XYZ"
    steps:
      - uses: actions/checkout@v3
      - run: bash a.sh
